# V30 新增
# 說明：這只能擋「遵守 robots.txt 規範」的爬蟲與搜尋引擎（例如 Googlebot），
# 對於刻意寫程式直接抓 data/*.json 網址的人沒有強制力（robots.txt 本質是君子協定，
# 不是存取控制）。這裡加上是為了：
#   1. 避免 data/*.json 這種原始資料檔案被搜尋引擎索引、外流到搜尋結果
#   2. 明確表達「這個路徑不開放給程式化存取」的意圖，作為後續主張權利的依據
User-agent: *
Disallow: /data/
